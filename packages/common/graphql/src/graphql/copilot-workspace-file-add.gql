mutation addUserFiles($blob: Upload!, $metadata: String) {
  addUserFiles(blob: $blob, metadata: $metadata) {
    fileId
    fileName
    blobId
    mimeType
    size
    metadata
    createdAt
  }
}
