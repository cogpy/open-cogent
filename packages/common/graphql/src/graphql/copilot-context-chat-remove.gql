query removeContextChat($contextId: String!, $sessionId: String!) {
  currentUser {
    copilot {
      contexts(contextId: $contextId) {
        removeContextChat(sessionId: $sessionId)
      }
    }
  }
}
