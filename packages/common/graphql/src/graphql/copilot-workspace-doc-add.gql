mutation addUserDocs($sessionId: String!, $title: String!, $content: String!, $metadata: String) {
  addUserDocs(content: $content, sessionId: $sessionId, title: $title, metadata: $metadata) {
    sessionId
    docId
    title
    content
    metadata
    createdAt
    updatedAt
  }
}
