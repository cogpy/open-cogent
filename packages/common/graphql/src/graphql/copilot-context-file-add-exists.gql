mutation addContextFile($contextId: String!, $blobId: String!) {
  addContextFile(content: $content, blobId: $blobId) {
    id
    createdAt
    name
    mimeType
    chunkSize
    error
    status
    blobId
  }
}
